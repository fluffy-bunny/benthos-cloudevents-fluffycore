input:
  gibberish:
    length: 2

pipeline:
  threads: 1
  processors:
  - sleep:
      duration: 1s
 
 

output:
  cloudevent_output: 
    grpc_url: "processor:9050"
    auth_type: "none"
    user_name: "admin"
    password: "password"